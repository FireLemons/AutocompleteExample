const domContainer = document.getElementById('app');

var exampleContents = <AbbreviationAutocomplete data={airportData} placeholder="Airport Code" />

ReactDOM.render(exampleContents, domContainer);
